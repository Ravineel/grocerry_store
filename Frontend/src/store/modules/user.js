// initial state
const state = () => ({
  user: [],
  isAuthenticated: false,
  token: null,
  errors: [],
  loading: false,
});

// getters
const getters = {};

// actions
const actions = {};

// mutations
const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
